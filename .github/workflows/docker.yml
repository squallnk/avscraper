name: Build and push image

# 构建多架构镜像推到 GHCR，Unraid 直接从 ghcr.io/<owner>/avscraper:latest 拉取。
# 首次推送后需要去 GitHub 仓库的 Packages 页面把包设为 Public，否则 Unraid 拉不到。

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

# **必须串行**。连着推两个提交时两条构建会并行跑，而它们都往 :latest 上推 ——
# 谁最后结束谁说了算。实测踩过：新构建 35s 结束、旧构建 98s 结束，
# 结果 latest 被旧的覆盖回去，用户 docker pull 拿到的还是旧镜像，
# 表现为「我明明拉了却不是最新版」。
concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 准备 Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录 GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 记录构建信息
        id: buildinfo
        run: echo "time=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

      - name: 计算标签
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=tag
            type=sha,prefix=sha-

      - name: 构建并推送
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            BUILD_SHA=${{ github.sha }}
            BUILD_TIME=${{ steps.buildinfo.outputs.time }}
          cache-from: type=gha
          cache-to: type=gha,mode=max